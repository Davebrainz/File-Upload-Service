# Local development uses server/users.json and server/uploads automatically.
# Add these variables in Vercel Preview and Production after connecting storage.
KV_REST_API_URL=
KV_REST_API_TOKEN=
BLOB_READ_WRITE_TOKEN=